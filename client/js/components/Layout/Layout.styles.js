import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#fff',
    height: '100%',
  },
  noteArea: {
    width: 300,
    alignSelf: 'center',
  },
})

export default styles
