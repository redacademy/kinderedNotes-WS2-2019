import React from 'react'
import {Text, SafeAreaView, ScrollView} from 'react-native'

const Note = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>heyyaa</Text>
      </ScrollView>
    </SafeAreaView>
  )
}

export default Note
