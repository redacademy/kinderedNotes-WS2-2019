import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  form: {
    width: 300,
    // height: '100%',
    // position: 'absolute',
    // bottom: '0px',
    // top: 0,
    marginLeft: 'auto',
    marginRight: 'auto',
  },
})

export default styles
